<main class="main_container">
  <h1 class="main_title"><%= pluralize(@searches.length, "search") %></h1>
  <div id="searches">
    <% @searches.each do |search| %>
      <%= render search %>
      <p>
        <%= link_to "Show this search", search %>
      </p>
    <% end %>
  </div>
</main>
